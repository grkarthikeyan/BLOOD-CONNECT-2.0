BLOOD CONNECT 2.0 - FULL FIXED PROJECT

This project was reconstructed from the uploaded BloodConnect2 project.
node_modules and .expo generated files were excluded to keep the ZIP manageable.

Main fixes:
- src/app/index.tsx is now a React Native Expo Router screen.
- src/components/HomeScreen.tsx contains only the React Native HomeScreen code; HTML/CSS sections were removed.

After extracting:
1. Open the project folder in VS Code.
2. Run: npm install
3. Run: npx expo start --clear
